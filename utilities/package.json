{
  "private": true,
  "name": "@hydrocut-trail-status/utilities",
  "version": "0.1.0",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w --preserveWatchOutput",
    "lint": "eslint -c ./.eslintrc src/**/*.ts"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
